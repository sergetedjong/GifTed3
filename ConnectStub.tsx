"use client";
/** 
 * Placeholder for RainbowKit / wagmi ConnectButton to keep the demo self-contained.
 * Swap with real providers when you add wagmi/RainbowKit.
 */
export default function ConnectStub() {
  return (
    <button className="btn">Connect Wallet (stub)</button>
  );
}
